# Repository boundary

This repository owns the reusable collector implementation and its practical
contract. It contains source code, synthetic tests, neutral examples, and
public maintenance documentation only. Operational records, deployment
bindings, result data, and provenance copies belong outside this repository.

`public-release.toml` defines the tracked-path boundary enforced by the
standalone privacy checker.
